mtcars

mean(mtcars$mpg)                                     #MPG Mean
sd(mtcars$mpg)                                       #MPG Std. Dev. 
max(mtcars$mpg)                                      #MPG Max
(max(mtcars$mpg)-mean(mtcars$mpg))/sd(mtcars$mpg)    #MPG Max Z score

mean(mtcars$cyl)                                     #CYL Mean
sd(mtcars$cyl)                                       #CYL Std. Dev.
max(mtcars$cyl)                                      #CYL Max
(max(mtcars$cyl)-mean(mtcars$cyl))/sd(mtcars$mpg)    #CYL Max Z score

mean(mtcars$disp)                                    #DISP Mean
sd(mtcars$disp)                                      #DISP Std. Dev.
max(mtcars$disp)                                     #DISP Max
(max(mtcars$disp)-mean(mtcars$disp))/sd(mtcars$disp) #DISP Max Z score

mean(mtcars$hp)                                      #HP Mean
sd(mtcars$hp)                                        #HP Std. Dev.
max(mtcars$hp)                                       #HP Max
(max(mtcars$hp)-mean(mtcars$hp))/sd(mtcars$hp)       #HP Max Z score

mean(mtcars$drat)                                    #DRAT Mean
sd(mtcars$drat)                                      #DRAT Std. Dev.
max(mtcars$drat)                                     #DRAT Max
(max(mtcars$drat)-mean(mtcars$drat))/sd(mtcars$drat) #DRAT Max Z score

mean(mtcars$wt)                                      #WT Mean
sd(mtcars$wt)                                        #WT Std. Dev.
max(mtcars$wt)                                       #WT Max
(max(mtcars$wt)-mean(mtcars$wt))/sd(mtcars$wt)       #WT Max Z score

mean(mtcars$qsec)                                    #QSEC Mean
sd(mtcars$qsec)                                      #QSEC Std. Dev.
max(mtcars$qsec)                                     #QSEC Max
(max(mtcars$qsec)-mean(mtcars$qsec))/sd(mtcars$qsec) #QSEC Max Z score

mean(mtcars$gear)                                    #GEAR Mean
sd(mtcars$gear)                                      #GEAR Std. Dev.
max(mtcars$gear)                                     #GEAR Max
(max(mtcars$gear)-mean(mtcars$gear))/sd(mtcars$gear) #GEAR Max Z score

mean(mtcars$carb)                                    #CARB Mean
sd(mtcars$carb)                                      #CARB Std. Dev.
max(mtcars$carb)                                     #CARB Max
(max(mtcars$carb)-mean(mtcars$carb))/sd(mtcars$carb) #CARB Max Z score


#Clear consolecat("\014")  # Clear console ctrl+L

